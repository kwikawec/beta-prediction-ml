# Data

Input data are not included in this repository. Empty CSV templates with the expected column names are provided in `data/templates/`.

Place the required input CSV files directly in this `data/` directory when running the code locally.
